#!/bin/bash
# For MBP touchbar mic toggle
pactl set-source-mute alsa_input.pci-0000_04_00.3.BuiltinMic toggle
if pactl get-source-mute alsa_input.pci-0000_04_00.3.BuiltinMic | grep -q yes; then
    notify-send "Mic Muted" -i microphone-off
else
    notify-send "Mic Unmuted" -i microphone
fi
