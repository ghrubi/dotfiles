-- MacBook Air 2017 pointer and touchpad tuning proven on the Quattro install.
-- Keyboard and portable touchpad preferences are defined by the common layer.
hl.config({
  input = {
    sensitivity = 0.35,
    accel_profile = "flat",
    touchpad = {
      scroll_factor = 0.4,
      drag_3fg = 1,
      tap_to_click = false,
    },
  },
})
